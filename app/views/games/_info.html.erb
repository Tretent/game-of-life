<div class="game-info">
  <p><strong><%= t("games.info.generation") %></strong>
    <span id="generation_count" data-generation="<%= game.current_generation %>"><%= game.current_generation %></span>
  </p>
  <p><strong><%= t("games.info.size") %></strong> <%= game_size_display(game.rows, game.columns) %></p>
</div>
