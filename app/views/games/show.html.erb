<h1><%= @game.name %></h1>

<%= render "info", game: @game %>

<div class="game-grid-container" id="game_grid">
  <%= render "grid", grid: @game.current_grid %>
</div>

<%= render "controls", game: @game %>

<p><%= link_to t(".back"), games_path %></p>
